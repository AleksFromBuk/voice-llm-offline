### 3. Глобальный `README.md`


# 🎤Голосовой транскриптор (офлайн)

Небольшое десктоп-приложение на Python (Tkinter), которое:

- записывает речь с микрофона;
- **офлайн** распознаёт её с помощью [`Vosk`](https://alphacephei.com/vosk/);
- по желанию улучшает текст (орфография, пунктуация, частичная нормализация)
  с помощью русской LLM **`cointegrated/rut5-small-normalizer`** (Hugging Face);
- показывает текст по мере речи и позволяет сохранить его в `.txt`.

Проект реализован в рамках тестового задания.

---

## 🔧 Технологии

- Python 3.10+
- Tkinter — GUI
- Vosk — офлайн распознавание речи (русский язык)
- sounddevice + numpy — захват и подготовка аудио
- transformers + torch + sentencepiece — LLM-модель RUT5-Normalizer
- Логирование в `transcriber.log`

---

## 📂 Структура проекта

```text
voice-llm-offline/
├─ app.py              # Основной код приложения (GUI + логика)
├─ requirements.txt    # Python-зависимости
├─ models/
│   └─ README.md       # Инструкция по установке Vosk-модели
└─ .gitignore          # Исключения (в т.ч. .venv и каталог моделей)
````

**Важно:** сами модели (`vosk-model-small-ru-0.22`, кеш HuggingFace и т.п.)
в репозиторий не добавляются — они скачиваются/распаковываются локально.

---

## 🖥 Требования

* ОС: Windows / Linux / macOS
* Python: 3.10 или новее
* Микрофон, корректно настроенный в системе
* Свободное место на диске:

  * ~1500+ МБ (до 2.5 Гб) (примерно) для LLM `rut5-small-normalizer` (скачивается один раз).

---

## 🚀 Установка и запуск

### 1. Клонировать репозиторий

```bash
git clone https://github.com/<your-account>/voice-llm-offline.git
cd voice-llm-offline
```

### 2. Создать виртуальное окружение и установить зависимости

```bash
python -m venv .venv
# Windows (PowerShell):
.venv\Scripts\Activate.ps1
# или cmd:
.venv\Scripts\activate.bat
# Linux / macOS:
# source .venv/bin/activate

pip install --upgrade pip
pip install -r requirements.txt
```

### 3. Скачать и установить Vosk-модель

1. Перейти на страницу моделей Vosk и скачать русскую модель, например:
   [`vosk-model-small-ru-0.22`](https://alphacephei.com/vosk/models/vosk-model-ru-0.22.zip).

2. Распаковать архив так, чтобы получилась структура:

```text
voice-llm-offline/
  models/
    vosk-model-small-ru-0.22/
      am/
      conf/
      graph/
      ...
```

Если вы скачивали модель с другим именем, либо переименуйте папку в
`vosk-model-small-ru-0.22`, либо поправьте путь в `app.py`
в методе `_load_models()`.

Подробнее — см. `models/README.md`.

### 4. Запуск (загрузка LLM)

```bash
  python  app.py
```

---

## 🕹 Использование приложения

1. Запустите `app.py`.
   Вверху окна появится прогресс-бар «Загрузка моделей…».
2. После загрузки:

   * кнопка **«🎤 Начать запись»** станет активной;
   * чекбокс **«Включить улучшение текста (LLM)»** будет включён по умолчанию.
3. Нажмите **«🎤 Начать запись»** и говорите в микрофон.

   * Сырые фразы Vosk подсвечиваются значком `🔹`.
   * После обработки LLM поверх них появляется строка с `✨` (если улучшение признано полезным).
4. Нажмите **«⏹️ Остановить запись»**, чтобы завершить текущую сессию.
5. Кнопка **«🗑️ Очистить»** очищает текстовое поле.
6. Кнопка **«💾 Сохранить»** открывает диалог сохранения текста в `.txt`.

---

## 🧠 Как работает улучшение текста (LLM)

* В качестве LLM используется специализированная модель
  [`cointegrated/rut5-small-normalizer`], заточенная на нормализацию текста.
* Для каждой распознанной фразы приложение решает, **нужно ли** вызывать LLM:

  * игнорирует очень короткие фразы;
  * обращает внимание на длинные предложения без знаков препинания;
  * учитывает наличие цифр, типичные «слепленные» слова и пропущенные предлоги.
* Если модель меняет текст слишком радикально (например, переписывает фразу),
  исходный текст сохраняется без изменений.

Таким образом LLM вызывается только там, где это действительно может помочь,
а не на каждое короткое слово.

---

## 📝 Известные ограничения

* Постановка запятых и нормализация чисел зависят от выбранной модели;
  результат не всегда идеален.
* Скорость загрузки LLM - одно из *узких* мест

---

## 📄 Лицензия

Использование Vosk и модели `cointegrated/rut5-small-normalizer` регулируется
их лицензиями. Перед коммерческим использованием уточняйте условия у авторов
соответствующих проектов.

