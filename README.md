
# Русский голосовой транскриптор (полностью офлайн)

Прототип для тестового задания: приложение, в котором пользователь нажимает кнопку, говорит в микрофон, а текст появляется на форме по мере речи.

Особенности:

- 🎙️ Распознавание речи **офлайн** с помощью [Vosk](https://alphacephei.com/vosk/)
- 🧠 Дополнительная обработка текста с помощью русской LLM (ruT5, `transformers`, `torch`) — модель иногда сглаживает фразы и слегка улучшает текст, но **не гарантирует** идеальную орфографию и пунктуацию
- 🖥️ Простой графический интерфейс на `tkinter`
- 🔌 Полностью локальная работа после первой загрузки LLM-модели

---

## 1. Стек технологий

- **Python 3.10+**
- **Vosk** — офлайн Speech-To-Text (STT)
- **sounddevice** — захват аудио с микрофона
- **numpy** — работа с аудио-буферами
- **transformers** + **torch** + **sentencepiece** — русскоязычная Seq2Seq-модель (ruT5)
- **protobuf<5** — для совместимости с Vosk
- **tkinter** — десктопный GUI

---

## 2. Требования

- Windows 10/11
- Python 3.10 (x64)
- Установленный `git` (опционально, для клонирования)
- Рабочий микрофон
- Свободное место:
  - ~50–100 МБ на модель Vosk
  - ~1–2 ГБ на LLM (ruT5 + PyTorch)

> ⚠️ Модели не хранятся в репозитории.  
> Vosk-модель скачивается вручную, LLM — автоматически через `transformers` при первом запуске.

---

## 3. Установка

### 3.1. Клонирование репозитория

```bash
  git clone https://github.com/<ВАШ-ЛОГИН>/voice-llm-offline.git
cd voice-llm-offline
````

(Либо скачать ZIP с GitHub и распаковать.)

### 3.2. Виртуальное окружение

```bash
  python -m venv .venv
```

Активировать:

* **Windows (PowerShell / CMD)**

  ```bash
  .venv\Scripts\activate
  ```

* **Linux/macOS**

  ```bash
  source .venv/bin/activate
  ```

### 3.3. Установка зависимостей

```bash
  pip install -r requirements.txt
```

`requirements.txt`:

```text
vosk
sounddevice
numpy
transformers
torch
protobuf<5
sentencepiece
```

---

## 4. Модели

### 4.1. Модель Vosk (русский)

1. Скачать модель Vosk для русского языка, например
   **`vosk-model-small-ru-0.22`**
   по ссылке: [https://alphacephei.com/vosk/models/vosk-model-ru-0.22.zip](https://alphacephei.com/vosk/models/vosk-model-ru-0.22.zip)

2. Распаковать архив в папку `models/` так, чтобы структура была:

   ```text
   voice-llm-offline/
   ├── app.py
   ├── requirements.txt
   ├── models/
   │   ├── README.md
   │   └── vosk-model-small-ru-0.22/
   │       ├── am/
   │       ├── conf/
   │       ├── graph/
   │       └── ...
   └── ...
   ```

3. В коде путь к модели по умолчанию — `models/vosk-model-small-ru-0.22`.
   Если изменить имя папки, нужно поправить путь в `app.py`.

### 4.2. Модель LLM (ruT5)

* Используется модель `cointegrated/rut5-small` из `transformers`.
* При первом запуске приложения она **скачивается автоматически** из Hugging Face в кеш пользователя.
* Интернет нужен **только при первом запуске** для загрузки весов; далее всё работает локально.

> ⚠️ Это небольшая русскоязычная модель общего назначения.
> Она **не является специализированным грамматическим корректором**, поэтому:
>
> * не всегда корректно расставляет знаки препинания;
> * иногда почти не меняет фразу;
> * в редких случаях может добавлять лишние слова.

В коде есть простая фильтрация типичных артефактов, но результат не гарантируется как «идеально грамотный текст».

---

## 5. Запуск приложения

В активированном виртуальном окружении:

```bash
python app.py
```

После запуска:

1. Откроется окно **«Русский голосовой транскриптор (полностью офлайн)»**.
2. Вверху будет отображаться статус загрузки моделей (Vosk + LLM).
3. После успешной загрузки кнопка **«Начать запись»** станет активной.
4. Нажмите **«Начать запись»** и говорите в микрофон.
5. Нажмите **«Остановить запись»**:

   * в верхней части текстового поля появится распознанный Vosk-текст;
   * под ним — строка `LLM: ...` с вариантом, обработанным LLM.

Кнопка **«Очистить»** удаляет содержимое текстового поля и сообщение статуса.

---

## 6. Структура проекта

```text
voice-llm-offline/
├── app.py                # Основной код GUI + захват аудио + Vosk + LLM
├── requirements.txt      # Python-зависимости
├── models/
│   └── README.md         # Инструкция по скачиванию и размещению модели Vosk
└── README.md             # Описание проекта
```

---

## 7. Как это работает

1. **Захват аудио**

   * `sounddevice.InputStream` читает звук с микрофона (mono, 16 kHz).
   * Сэмплы складываются в `queue.Queue`.

2. **Распознавание речи (Vosk)**

   * Фоновый поток читает данные из очереди.
   * Передаёт их в `vosk.KaldiRecognizer`.
   * Формируются промежуточные и финальные результаты.
   * Итоговый текст отображается как результат распознавания.

3. **Дополнительная обработка текста (LLM)**

   * Финальный текст Vosk отправляется в ruT5:

     * `AutoTokenizer` → токенизация;
     * `AutoModelForSeq2SeqLM.generate` → генерация варианта;
     * декодирование и небольшой пост-процессинг (обрезка типичных артефактов).
   * Результат выводится в GUI с префиксом `LLM:`.

4. **GUI (tkinter)**

   * Главное окно работает в основном потоке.
   * Захват аудио и распознавание речи выполняются в отдельных потоках, чтобы интерфейс не «подвисал».
   * Статусная строка снизу показывает состояние: «Запись…», сообщения об ошибках и т.п.

---

## 8. Ограничения

* LLM не гарантирует:

  * строгую грамматическую корректность;
  * полноценную расстановку пунктуации;
  * отсутствие артефактов в виде лишних слов.
* Прототип ориентирован на короткие фразы, а не длинные монологи.
* Интерфейс минимален и сделан именно как демонстрация прототипа для тестового задания.

---

## 9. Быстрый чек-лист для проверяющего

1. Установлен Python 3.10 (x64).

2. Создано и активировано виртуальное окружение.

3. Выполнено:

   ```bash
   pip install -r requirements.txt
   ```

4. В `models/` распакована папка `vosk-model-small-ru-0.22/`.

5. Запуск:

   ```bash
   python app.py
   ```

6. В окне:

   * Нажать **«Начать запись»**, произнести короткую фразу.
   * Нажать **«Остановить запись»**.
   * Увидеть:

     * исходный текст от Vosk;
     * строку `LLM: ...` с вариантом, предложенным LLM.

